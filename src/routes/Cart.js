import Header from "../components/Header";
import Layout from "../components/Layout";

const Cart = (props) => {
  return (
    <Layout>
      <Header>Cart</Header>
    </Layout>
  );
};

export default Cart;

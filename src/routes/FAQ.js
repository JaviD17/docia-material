import Header from "../components/Header";
import Layout from "../components/Layout";

const FAQ = (props) => {
  return (
    <Layout>
      <Header>FAQ</Header>
    </Layout>
  );
};

export default FAQ;

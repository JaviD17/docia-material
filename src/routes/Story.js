import Header from "../components/Header";
import Layout from "../components/Layout";

const Story = (props) => {
  return (
    <Layout>
      <Header>Story</Header>
    </Layout>
  );
};

export default Story;

import Header from "../components/Header";
import Layout from "../components/Layout";

const MealPlans = (props) => {
  return (
    <Layout>
      <Header>MealPlans</Header>
    </Layout>
  );
};

export default MealPlans;
